/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // allowedDevOrigins: [
  //   "per-functionality-competitions-optical.trycloudflare.com",
  // ],
};

export default nextConfig;
